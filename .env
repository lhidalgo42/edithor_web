# Environment variables for Edithor Website

# CAPTCHA Configuration (Example using generic names)
# Replace with your actual CAPTCHA provider's keys
VITE_CAPTCHA_SITE_KEY="YOUR_CAPTCHA_SITE_KEY_HERE"
CAPTCHA_SECRET_KEY="YOUR_CAPTCHA_SECRET_KEY_HERE" # Note: Secret keys should NOT be exposed client-side (VITE_ prefix makes it available)

# Email Sending Configuration (Example using generic SMTP)
# Replace with your actual SMTP credentials
SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_USER="your_email@example.com"
SMTP_PASS="your_email_password"
EMAIL_RECIPIENT="recipient_email@example.com" # Email address to send contact form submissions to

# IMPORTANT:
# - Prefix variables with VITE_ to expose them to your Vite client-side code.
# - DO NOT commit this file to version control if it contains sensitive information.
# - The CAPTCHA_SECRET_KEY and SMTP credentials should ideally be used in a backend function, not directly in the frontend.
